import React, { useState } from 'react';
import { Calendar, MapPin, Clock, Users, ArrowRight, Award, Presentation, Globe, ChatCircleDots } from 'lucide-react';

const App = () => {
  const [activeTab, setActiveTab] = useState('main'); // 'main' | 'roomA' | 'roomB'

  const scheduleData = [
    {
      time: "10:30 - 11:20",
      tag: "오프닝 세션",
      title: "개회식, 시상식 & 우수 논문 발표",
      description: "학술제 개막과 우수 연구 발표 및 시상으로 하루의 논의를 시작합니다.",
      type: "event",
      color: "border-blue-500",
      bg: "bg-blue-50/50"
    },
    {
      time: "11:30 - 13:00",
      tag: "특별 강연",
      title: "초청 강연 · 시대예보: 경량문명의 탄생",
      speaker: "송길영 마인드마이너",
      description: "데이터로 읽는 사회 변화와 직능의 미래를 이야기합니다.",
      type: "keynote",
      color: "border-indigo-500",
      bg: "bg-indigo-50/50"
    },
    {
      time: "14:00 - 16:00",
      tag: "국제 학술 심포지엄",
      title: "돌봄과 약료, 세계는 지금",
      description: "대만, 일본, 유럽/미국의 선진 사례를 통해 약료 사업의 미래를 조망합니다.",
      isComplex: true,
      subSessions: [
        { title: "대북시 가정약사 제도 현안과 성공 사례", speaker: "왕명원 대북시약사공회 국제이사" },
        { title: "일본 단골약사제도를 통한 약료발전방향", speaker: "이누부세 히로오 동경도약제사회" },
        { title: "유럽, 미국 등 다제약물 관리 현황", speaker: "이주연 서울대 약학대학 교수" }
      ],
      type: "symposium",
      color: "border-emerald-500",
      bg: "bg-emerald-50/50"
    },
    {
      time: "16:00 - 17:00",
      tag: "종합 토의",
      title: "2부 종합토의",
      description: "보건복지부, 서울시, 건보공단 등 주요 관계자가 함께하는 패널 토론입니다.",
      panels: ["보건복지부", "서울특별시", "건보공단", "지자체", "서울시약사회"],
      type: "discussion",
      color: "border-slate-400",
      bg: "bg-slate-50"
    }
  ];

  return (
    <div className="min-h-screen bg-[#f9fafb] text-slate-900 font-sans selection:bg-blue-100">
      {/* Header Section (SPAF 스타일) */}
      <div className="bg-[#0f172a] text-white pt-20 pb-24 px-6 relative overflow-hidden">
        <div className="absolute top-0 left-0 w-full h-full opacity-20">
          <div className="absolute top-[-10%] right-[-5%] w-[400px] h-[400px] bg-blue-600 rounded-full blur-[120px]"></div>
          <div className="absolute bottom-[-20%] left-[-10%] w-[300px] h-[300px] bg-indigo-600 rounded-full blur-[100px]"></div>
        </div>
        
        <div className="max-w-4xl mx-auto relative z-10 text-center">
          <div className="inline-flex items-center gap-2 bg-blue-500/10 border border-blue-500/20 px-4 py-1.5 rounded-full text-blue-400 text-sm font-bold mb-6">
            <Calendar size={14} />
            <span>2026. 05. 31 (SUN)</span>
          </div>
          <h1 className="text-4xl md:text-6xl font-black mb-6 tracking-tight">
            SPAF <span className="text-blue-500">2026</span>
          </h1>
          <p className="text-slate-400 text-lg md:text-xl font-medium break-keep max-w-2xl mx-auto">
            서울특별시약사회 학술제 및 국제 학술 심포지엄 일정 안내
          </p>
        </div>
      </div>

      {/* Main Schedule Content */}
      <div className="max-w-4xl mx-auto px-4 -mt-12 mb-20 relative z-20">
        
        {/* Navigation Tabs (SPAF 스타일 탭) */}
        <div className="bg-white p-2 rounded-2xl shadow-xl mb-10 flex gap-1 border border-slate-100">
          <button 
            onClick={() => setActiveTab('main')}
            className={`flex-1 py-3 rounded-xl font-bold transition-all text-sm md:text-base ${activeTab === 'main' ? 'bg-slate-900 text-white shadow-lg' : 'text-slate-500 hover:bg-slate-50'}`}
          >
            대강당 (Main)
          </button>
          <button 
            onClick={() => setActiveTab('roomA')}
            className={`flex-1 py-3 rounded-xl font-bold transition-all text-sm md:text-base ${activeTab === 'roomA' ? 'bg-slate-900 text-white shadow-lg' : 'text-slate-500 hover:bg-slate-50'}`}
          >
            분과 1 (Room A)
          </button>
          <button 
            onClick={() => setActiveTab('roomB')}
            className={`flex-1 py-3 rounded-xl font-bold transition-all text-sm md:text-base ${activeTab === 'roomB' ? 'bg-slate-900 text-white shadow-lg' : 'text-slate-500 hover:bg-slate-50'}`}
          >
            분과 2 (Room B)
          </button>
        </div>

        {/* Timeline Grid */}
        <div className="space-y-6">
          {scheduleData.map((item, idx) => (
            <div key={idx} className="group relative flex flex-col md:flex-row gap-4 md:gap-8 bg-white p-5 md:p-8 rounded-3xl border border-slate-100 shadow-sm hover:shadow-md transition-all">
              
              {/* Left Side: Time & Tag */}
              <div className="w-full md:w-40 shrink-0 border-b md:border-b-0 md:border-r border-slate-100 pb-3 md:pb-0">
                <div className="flex items-center gap-2 text-blue-600 font-bold text-lg mb-1">
                  <Clock size={18} className="md:hidden" />
                  <span>{item.time.split(' - ')[0]}</span>
                  <span className="text-slate-300 text-sm font-normal mx-1">-</span>
                  <span className="text-slate-400 text-sm md:text-base">{item.time.split(' - ')[1]}</span>
                </div>
                <div className={`inline-block px-2 py-0.5 rounded text-[10px] font-bold uppercase tracking-wider ${item.color.replace('border-', 'text-')} bg-slate-50`}>
                  {item.tag}
                </div>
              </div>

              {/* Right Side: Content */}
              <div className="flex-1">
                <div className="flex items-start justify-between gap-4 mb-2">
                  <h3 className="text-xl md:text-2xl font-bold text-slate-800 break-keep group-hover:text-blue-600 transition-colors">
                    {item.title}
                  </h3>
                  <div className="hidden md:block">
                    {item.type === 'keynote' && <Award className="text-indigo-500" />}
                    {item.type === 'symposium' && <Globe className="text-emerald-500" />}
                  </div>
                </div>

                {item.speaker && (
                  <div className="flex items-center gap-2 mb-3">
                    <div className="w-6 h-6 rounded-full bg-slate-100 flex items-center justify-center text-slate-500">
                      <Users size={12} />
                    </div>
                    <span className="text-sm font-bold text-slate-700">{item.speaker}</span>
                  </div>
                )}

                <p className="text-slate-500 text-sm md:text-base mb-4 leading-relaxed break-keep">
                  {item.description}
                </p>

                {/* Sub-sessions (Symposium Only) */}
                {item.isComplex && (
                  <div className="space-y-3 bg-slate-50 p-4 rounded-2xl border border-slate-100 mt-4">
                    {item.subSessions.map((sub, sIdx) => (
                      <div key={sIdx} className="flex gap-3">
                        <div className="mt-1 text-emerald-500"><ArrowRight size={14} /></div>
                        <div>
                          <div className="text-sm md:text-base font-bold text-slate-800 leading-snug">{sub.title}</div>
                          <div className="text-xs text-slate-500 mt-0.5">{sub.speaker}</div>
                        </div>
                      </div>
                    ))}
                  </div>
                )}

                {/* Panels (Discussion Only) */}
                {item.panels && (
                  <div className="flex flex-wrap gap-1.5 mt-4">
                    {item.panels.map((p, pIdx) => (
                      <span key={pIdx} className="px-2 py-1 bg-white border border-slate-200 rounded-md text-[10px] md:text-xs text-slate-600 font-medium">
                        {p}
                      </span>
                    ))}
                  </div>
                )}
              </div>
            </div>
          ))}
        </div>

        {/* Info Footer */}
        <div className="mt-12 p-8 bg-slate-900 rounded-[2.5rem] text-center shadow-2xl relative overflow-hidden">
            <div className="relative z-10">
                <h4 className="text-white font-bold mb-2">행사 장소 및 문의</h4>
                <div className="flex flex-col md:flex-row justify-center items-center gap-4 text-slate-400 text-sm">
                    <div className="flex items-center gap-1.5">
                        <MapPin size={16} className="text-blue-500" />
                        <span>코엑스 컨퍼런스룸 4F</span>
                    </div>
                    <div className="hidden md:block text-slate-700">|</div>
                    <div className="flex items-center gap-1.5">
                        <ChatCircleDots size={16} className="text-blue-500" />
                        <span>02-123-4567</span>
                    </div>
                </div>
            </div>
        </div>
      </div>
    </div>
  );
};

export default App;
